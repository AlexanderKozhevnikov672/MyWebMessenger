# MyWebMessenger

### TODO:

- Improve frontend part (used only simple htmls)
- Use sessions instead of `jwt.Token` with cookies for better security
- Remove local storage of socket connections for scalability capability
- Improve admin part (in general)
